
spring:
  datasource:
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/mall_ums?characterEncoding=UTF8&autoReconnect=true&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      # 主键自增
      id-type: auto
server:
  port: 8000
